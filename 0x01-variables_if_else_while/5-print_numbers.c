#include <stdio.h>

/**
 *  * main - prints all single digit numbers of base
 *   * 10 starting from 0, followed by a new line
 *     */
int main(void)
{
	int no 

		for (no = 0; no <= 9; no++)

		printf("%d", no);
		printf("\n");	
			
		return (0);
}
