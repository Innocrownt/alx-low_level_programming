#include <stdio.h>
/**
 *  * main - main function
 *   *
 *    * Return: end program
 *     */
int main(void)
{
	char ch;
		char CH;

			for (ch = 'a'; ch <= 'z'; ++ch){
			putchar("%c", ch);
  		}
			for (CH = 'A'; CH <= 'Z'; ++CH){
			putchar("%c", CH);}

		putchar("\n");

		return (0);
}
