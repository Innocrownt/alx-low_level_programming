#include <stdio.h>
/**
 *  * main - main function
 *   *
 *    * Return: end program
 *     */
int main(void)
{
	char cha;
		char CHA;

			for (cha = 'a'; cha <= 'z'; ++cha)
			putchar("%c", cha);

			for (CHA = 'A'; CHA <= 'Z'; ++CHA)
			putchar("%c", CHA);

		putchar("\n");

		return (0);
}
