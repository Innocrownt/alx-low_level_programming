#include <stdio.h>
/**
 *  * main - main function
 *   *
 *    * Return: end program
 *     */
int main(void)
{
	char ch;
		char CH;

			for (ch = 'a'; ch <= 'z'; ++ch){
			printf("%c", ch);
  		}
			for (CH = 'A'; CH <= 'Z'; ++CH){
			printf("%c", CH);}

		printf("\n");

		return (0);
}
